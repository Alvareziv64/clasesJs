
// bucle for 

/* 
let usuarioSis = "admin";
let passwordSis = 123456;


for (let i = 0; i < 3; i = i + 1){

      let usuario = prompt("Ingrese su usuario");
      let password = prompt("Ingrese su password");

      if( usuarioSis == usuario && passwordSis == password ) {
          console.log ("Bienvenido al sistema");
          break;
      }

      else( console.log ("Error")
           )

}

*/ 

// bucle while 

/*

let voto = prompt ("Cual fue su juego de Pokémon favorito? Ingrese Red o Blue");
let votoRed = 0;
let votoBlue = 0;

while ( voto != "ESC" ) {

    if ( voto == "Red" || voto == "red") {
        
        votoRed = votoRed + 1; 
        
    }

    else if ( voto == "Blue" || voto == "blue") {

        votoBlue = votoBlue + 1;
    }

    else {
        console.log("Voto nulo");
    }

    voto = prompt ("Cual fue su juego de Pokémon favorito? Ingrese Red o Blue");

}

console.log ("Votos de Red: " + votoRed );
console.log ("Votos de Blue: " + votoBlue);

if ( votoRed > votoBlue ) {
    console.log("¡Gano Pokémon Red!")
}

else {
    console.log("¡Gano Pokémon Blue")
}

*/ 
